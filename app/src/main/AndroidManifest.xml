<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Save_Pets"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Hacer que LoginActivity se abra primero -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".RegistroActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RegistroMascotaActivity" />
        <activity android:name=".CuidadoHigieneActivity" />
        <activity android:name=".AlimentacionNutricionActivity" />
        <activity android:name=".OpinionActivity" />
        <activity android:name=".VerMascotasActivity" />
        <activity android:name=".EditarMascotaActivity" />
        <activity android:name=".MapaCentrosBellezaActivity"/>
        <activity android:name=".ChatbotActivity" />

    </application>
</manifest>